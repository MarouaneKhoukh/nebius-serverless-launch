Build a standalone Nebius Serverless Endpoint project for `hexgrad/Kokoro-82M`. Deliver a reproducible `Dockerfile`, Python server, `README.md`, environment example, verification and cleanup scripts, deterministic tests, and GitHub Actions.

Expose an OpenAI-compatible `POST /v1/audio/speech` route with MP3 and WAV output. Validate speed, voice, language, and response format; include the English phonemizer runtime and `ffmpeg`; and return clear JSON errors. Document the published image `cr.eu-north1.nebius.cloud/e00gw2b7v3pxetvpy7/kokoro-serve:d315ae1`, one-L40S deployment through a pre-filled Console link and equivalent CLI command, authentication, image rebuilding, troubleshooting, and cleanup.

Read the endpoint URL and optional token from environment variables. The verifier must save returned audio, validate status, content type, minimum size, and file signature, then write a JSON report. Keep tests credential-, network-, and GPU-free, make live testing opt-in, never expose credentials, and never fabricate cost, timing, or success evidence.
