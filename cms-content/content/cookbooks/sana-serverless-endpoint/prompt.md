Build a standalone Sana text-to-image project for Nebius Serverless Endpoints. Deliver a pinned `Dockerfile`, `serve.py`, `README.md`, `.env.example`, `.gitignore`, `LICENSE`, `ATTRIBUTION.md`, verification and cleanup scripts under `scripts/`, deterministic unit tests under `tests/`, and a GitHub Actions workflow.

Serve `Efficient-Large-Model/Sana_1600M_1024px_diffusers` with FastAPI on port 8000. Implement `GET /v1/models` and OpenAI-shaped `POST /v1/images/generations`. Default to base64 PNG output, serialize GPU access, reject unsupported response formats, bound image count, require dimensions between 256 and 2048 that are divisible by 32, and bound inference steps. Keep the Sana text encoder and VAE in bf16 while the transformer uses fp16. Pin all Python and base-image versions.

The README must provide a pre-filled Nebius deployment link and equivalent CLI command for one preemptible `gpu-l40s-a` / `1gpu-8vcpu-32gb` endpoint with port 8000, a 500 GiB disk, and 16 GiB shared memory. Read the endpoint URL from `BASE_URL`, optional endpoint authentication from `API_TOKEN`, and optional Hub authentication from `HF_TOKEN`; never commit or print credentials.

The verifier must wait for readiness, request a seeded 1024×1024 image, validate the decoded PNG, save `sana.png`, and write `run-report.json` with `"status": "passed"`. The cleanup script must require `ENDPOINT_ID`. Keep local tests network-free and GPU-free by stubbing heavyweight imports. Document security, troubleshooting, evidence collection, image rebuilding, and cleanup, and preserve Apache-2.0 upstream attribution.
