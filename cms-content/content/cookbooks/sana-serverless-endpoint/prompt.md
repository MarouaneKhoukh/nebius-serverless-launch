Build a standalone Sana project for Nebius Serverless Endpoints. Deliver a pinned `Dockerfile`, `serve.py`, `README.md`, environment example, license and attribution files, verification and cleanup scripts, deterministic tests, and GitHub Actions.

Serve `Efficient-Large-Model/Sana_1600M_1024px_diffusers` with FastAPI on port 8000. Implement `GET /v1/models` and `POST /v1/images/generations`, returning base64 PNG by default. Bound image count, dimensions, and inference steps; serialize GPU access; reject unsupported formats; and use bf16 for the text encoder and VAE with fp16 for the transformer.

Document a pre-filled Console link and equivalent CLI command for one preemptible L40S endpoint, authentication, rebuilding, troubleshooting, verification, and cleanup. Read `BASE_URL`, optional `API_TOKEN`, and optional `HF_TOKEN` from the environment. The verifier must request a seeded 1024×1024 image, validate and save `sana.png`, and write `run-report.json` with `"status": "passed"`. Keep tests credential-, network-, and GPU-free; never fabricate live results.
