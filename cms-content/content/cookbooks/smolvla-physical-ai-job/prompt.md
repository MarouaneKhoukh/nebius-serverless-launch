Build a standalone Nebius GPU-job project for a bounded SmolVLA training smoke test with official Hugging Face LeRobot tooling. Deliver job creation, verification, cleanup, and check scripts, a complete `README.md`, deterministic tests, and GitHub Actions.

Pin LeRobot to commit `bf31dd794ffb4f87380aba3912f64421e8352d3c` and dataset `lerobot/svla_so100_pickplace` to revision `728583b5eaf9e739a7f119e2def466fa1d552402`. Use the official training CLI, one L40S, batch size 1, a fixed seed, and exactly 50 steps. Copy the completed checkpoint to mounted Object Storage only after training succeeds.

Document why the unavailable original Parquet source was replaced, licensing, authentication, least-privilege storage setup, submission, logs, verification, troubleshooting, price checking, and cleanup. The offline verifier must validate policy metadata and non-empty model weights and write a JSON report. Keep local tests credential- and GPU-free, make live execution opt-in, distinguish pipeline validation from robot-safety evaluation, and never fabricate results.
