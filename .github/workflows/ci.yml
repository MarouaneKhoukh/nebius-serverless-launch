name: CI
on:
  push:
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate CMS source
        run: python3 scripts/check_content.py
      - name: Validate SmolVLA template
        working-directory: smolvla-finetune-job
        run: bash scripts/check.sh
